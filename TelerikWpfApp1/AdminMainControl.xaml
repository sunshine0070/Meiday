<UserControl x:Class="TelerikWpfApp1.AdminMainControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TelerikWpfApp1"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="600" Background="White">
    <Grid>
        <StackPanel Orientation="Horizontal" Height="35" VerticalAlignment="Top" HorizontalAlignment="Center">
            <Button Content="Page 1" MinWidth="100"
                    Command="{Binding SwitchViewCommand}" />
            <Button Content="Page 2" MinWidth="100" Margin="10 0 0 0"
                    Command="{Binding SwitchViewCommand}" />
            <Button Content="Page 3" MinWidth="100" Margin="10 0 0 0"
                    Command="{Binding SwitchViewCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
