<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Meiday.View"
             xmlns:vm="clr-namespace:Meiday.ViewModel"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" x:Class="Meiday.View.AdminSwitch3"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1050" Background="WhiteSmoke" FontFamily="SeoulNamsan B">
    <UserControl.Resources>
        <vm:AdminInsertPtViewModel x:Key="AdminInsertPtViewModel" />
        <Style TargetType="{x:Type Label}" x:Key="lblstyle">
            <Setter Property="Width" Value="100"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="White" />
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="10 0 10 0"/>
        </Style>
        <Style TargetType="{x:Type Border}" x:Key="borderstyle">
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Margin" Value="10" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Height" Value="60" />
            <Setter Property="Width" Value="400"/>
        </Style>
        <Style TargetType="{x:Type TextBox}" x:Key="tbstyle">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="35"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel 
            Background="#00498c" 
            Grid.Column="0"
            Height="650">
            <TextBlock 
                TextAlignment="Center" 
                FontSize="36" 
                FontWeight="Bold" 
                Text="관리자 등록" 
                Margin="0 30 0 0"
                Foreground="White"/>
            <!-- Insert Patient -->
            <StackPanel 
                    HorizontalAlignment="Left" 
                    DataContext="{StaticResource AdminInsertPtViewModel}"
                    Margin="20">
                <Label x:Name="label_Copy" 
                        Foreground="White"
                        FontWeight="Bold"
                        FontSize="24"
                        Content="사용자 추가" />

                <Border Style="{StaticResource borderstyle}">
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label
                                FontWeight="Bold"
                                x:Name="lblName"
                                Style="{StaticResource lblstyle}"
                                Content="이　　　　름" />
                        <TextBox
                                x:Name="tbName" 
                                Style="{StaticResource tbstyle}"
                                Text="{Binding name}" />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource borderstyle}">
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label
                                x:Name="lblAge"
                                Style="{StaticResource lblstyle}"
<<<<<<< Updated upstream
                                Content="나　　　　이" />
=======
                                Content="면  허  번  호" />
>>>>>>> Stashed changes
                        <TextBox 
                                x:Name="tbAge"
                                Style="{StaticResource tbstyle}"
                                Text="{Binding age}"  />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource borderstyle}">
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label 
                                x:Name="lblRegNum" 
                                Style="{StaticResource lblstyle}"
                                Content="주민등록번호" />
                        <TextBox 
                                x:Name="tbRegNum" 
                                Style="{StaticResource tbstyle}"
                                Text="{Binding regNum}"  />
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource borderstyle}">
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label 
                            x:Name="lblAddr" 
                            Style="{StaticResource lblstyle}"
                            Content="주　　　　소" />
                        <TextBox 
                            x:Name="tbAddr" 
                            Style="{StaticResource tbstyle}"
                            Text="{Binding addr}"  />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource borderstyle}">
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <Label
                                x:Name="lblPhone" 
                                Style="{StaticResource lblstyle}"
                                Content="연　  락 　처" />
                        <TextBox 
                                x:Name="tbPhone"
                                Style="{StaticResource tbstyle}"
                                Text="{Binding phone}"  />
                    </StackPanel>
                </Border>

                <telerik:RadButton 
                        x:Name="btSave" 
                        Content="저장" 
                        FontWeight="Bold"
                        Margin="10" 
                        Height="50" 
                        Width="250"
                        FontSize="20"
                        HorizontalAlignment="Center"
                        CornerRadius="10"
                        Command="{Binding AddPatient}" />
            </StackPanel>
        </StackPanel>


        <StackPanel 
            Grid.Column="1"
            Margin="20">
            <!-- inquire Admin List-->
            <StackPanel Height="300">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="30 10 310 10"  Foreground="Black"
                    FontSize="24" FontWeight="Bold"
                    Text="환자 리스트" />
                    <telerik:RadButton Width="100" Height="30"
                    FontSize="16" FontWeight="Bold"
                    Content="조회"
                    CornerRadius="10"
                    Command="{Binding LoadPatient}"/>
                </StackPanel>

                <ListView 
                    Width="550" Height="240" Margin="10" FontSize="16"
                    DataContext="{StaticResource AdminInsertPtViewModel}"
                    ItemsSource="{Binding SampleDatas, Mode=TwoWay, NotifyOnSourceUpdated=True, NotifyOnTargetUpdated=True}"
                    ScrollViewer.VerticalScrollBarVisibility="Visible"
                    ScrollViewer.CanContentScroll="False">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn 
                                DisplayMemberBinding="{Binding IdNum}"
                                Width="70"
                                Header="등록번호" />
                            <GridViewColumn 
                                DisplayMemberBinding="{Binding Name}"
                                Width="60"
                                Header="이름" />
                            <GridViewColumn 
                                DisplayMemberBinding="{Binding Age}"
                                Width="40"
                                Header="나이" />
                            <GridViewColumn 
                                DisplayMemberBinding="{Binding RegNum}"
                                Width="130"
                                Header="주민등록번호" />
                            <GridViewColumn 
                                DisplayMemberBinding="{Binding Phone}"
                                Width="120"
                                Header="연락처" />
                            <GridViewColumn 
                                DisplayMemberBinding="{Binding Addr}"
                                Width="100"
                                Header="주소" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </StackPanel>
            
            
            <!-- inquire Admin List-->
            <StackPanel Height="300">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="30 20 280 10"  Foreground="Black"
                    FontSize="24" FontWeight="Bold"
                    Text="관리자 리스트" />
                    <telerik:RadButton Width="100" Height="30"
                    FontSize="16" FontWeight="Bold"
                    Content="조회"
                    CornerRadius="10"
                    DataContext="{StaticResource AdminInsertPtViewModel}"
                    Command="{Binding LoadAdmin}"/>
                </StackPanel>

                <ListView 
                    Width="550" Height="240" Margin="10" FontSize="14"
                    DataContext="{StaticResource AdminInsertPtViewModel}"
                    ItemsSource="{Binding SampleAdmins, Mode=TwoWay, NotifyOnSourceUpdated=True, NotifyOnTargetUpdated=True}"
                    ScrollViewer.VerticalScrollBarVisibility="Visible"
                    ScrollViewer.CanContentScroll="False">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn 
                                DisplayMemberBinding="{Binding DrLicense}"
                                Width="80"
                                Header="면허번호" />
                            <GridViewColumn 
                                DisplayMemberBinding="{Binding DrName}"
                                Width="60"
                                Header="이름" />
                            <GridViewColumn 
                                DisplayMemberBinding="{Binding DrDeptName}"
                                Width="100"
                                Header="부서" />
                            <GridViewColumn 
                                DisplayMemberBinding="{Binding DrPosition}"
                                Width="80"
                                Header="직급" />
                            <GridViewColumn 
                                DisplayMemberBinding="{Binding DrEmail}"
                                Width="160"
                                Header="이메일" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</UserControl>
